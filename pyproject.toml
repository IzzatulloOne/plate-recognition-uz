[project]
name = "anpr-uz"
version = "1.0.0"
description = "ANPR Узбекистан: YOLO11 (детекция пластин) + TPS-ResNet-BiLSTM-CTC (best_accuracy.pth), FastAPI + WebSocket"
requires-python = ">=3.10,<3.14"
dependencies = [
    "torch>=2.4,<2.10",
    "torchvision>=0.19",
    "ultralytics>=8.3",
    "opencv-python-headless>=4.10",
    "numpy>=1.26,<2.3",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "pydantic>=2.9",
    "pydantic-settings>=2.7",
    "python-multipart>=0.0.18",
    "pillow>=10.4",
    "huggingface-hub>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.3", "httpx>=0.28", "ruff>=0.8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

# Без явного списка setuptools пытается угадать пакеты по корню проекта и падает
# на "Multiple top-level packages discovered": рядом лежат data/ и models/.
[tool.setuptools.packages.find]
include = ["app*", "tools*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # Depends() в аргументах — это норма для FastAPI

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
