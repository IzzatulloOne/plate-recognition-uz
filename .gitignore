.venv/
__pycache__/
*.py[cod]
.env
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# данные рантайма и всё скачанное: база, снапшоты, выборки для замеров,
# датасеты для дообучения. Папки создаются приложением сами.
data/
runs/

# веса: качаются скриптом (tools/fetch_yolo.py), в репозиторий не кладём
models/*.pt
models/*.onnx
# best_accuracy.pth — 196 МБ, храните в LFS или рядом с деплоем
best_accuracy.pth

# рабочие заметки и логи отладки — не для репозитория
aaa*.txt
*.bak
